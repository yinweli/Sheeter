// generated by sheeter, DO NOT EDIT.

package sheeter

import (
	"encoding/json"
	"os"
	"strconv"
)

type VerifyDataReader map[int64]VerifyData

var Json = "json/verifyData.json"

func FromJsonFile(path string) (reader VerifyDataReader, err error) {
	data, err := os.ReadFile(path)

	if err != nil {
		return nil, err
	}

	return FromJsonBytes(data)
}

func FromJsonBytes(data []byte) (reader VerifyDataReader, err error) {
	temps := map[string]VerifyData{}

	if err := json.Unmarshal(data, &temps); err != nil {
		return nil, err
	}

	datas := VerifyDataReader{}

	for key, value := range temps {
		k, err := strconv.ParseInt(key, 10, 64)

		if err != nil {
			return nil, err
		}

		datas[k] = value
	}

	return datas, nil
}
